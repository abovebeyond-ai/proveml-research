node_modules/
.DS_Store
*.aux
*.log
*.out
# LaTeX build artifacts. proveml-spec.bbl is the exception: arXiv does not run
# bibtex, so the compiled bibliography has to ship with the source.
*.bbl
!paper/proveml-spec.bbl
*.blg
*.fls
*.fdb_latexmk
*.pickle
npm-debug.log*
experiments/agent-adoption-results/
.playwright-mcp/
.tmp-convergence*.txt
experiments/.tmp-*

# Runs against the withdrawn education dataset: kept locally for reference,
# never published — the responses quote figures derived from real pupil records.
experiments/archive/

# Lean build output
formal/.lake/
